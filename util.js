const calculateTotalPrice = {
  calculate: (itemQuantity, itemPrice) => {
    return `total price is $${itemQuantity * itemPrice}`;
  },
};

export const author = 'jose';

export default calculateTotalPrice;
