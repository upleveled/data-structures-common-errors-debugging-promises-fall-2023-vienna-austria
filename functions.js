// All this variables are declared in the global scope
// let a = 123;
// const b = 123;
// var c = 123;

// declare a function as a variable
const myFunction = function () {
  return 'hello';
};

// use function declaration
function myNewFunction(/* Parameters */) {
  // body of the function

  return 'hello again '; // the output or the return value of the function
}

// console.log(myNewFunction());

const myAnimal = {
  type: 'dog',
  name: 'lulu',
  bark: () => 'woof',
};
